name: CI
on: [pull_request]

jobs:
#  linters:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v1
#      - name: Install Dependencies
#        run: yarn
#      - name: Formatting
#        run: yarn format
#      - name: CSS
#        run: yarn lint:css
##        continue-on-error: true
#        id: css
#      - name: JS
#        run: yarn lint:js
##        continue-on-error: true
#        id: js
##      - name: Check on failures
##        if: job.steps.css.status == failure() || job.steps.js.status == failure()
##        run: exit 1

#  tests:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v1
#      - name: Install Dependencies
#        run: yarn
#      - name: Unit
#        run: yarn test:unit
##        continue-on-error: true
#        id: unit
##      - name: Check on failures
##        if: job.steps.unit.status == failure()
##        run: exit 1
  deploy:
#    needs: [linters, tests]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install Dependencies
        run: yarn
      - uses: actions/checkout@v1
        env:
          NETLIFY_CMD: yarn build
        with:
          GITHUB_TOKEN: ${{ secrets.NETLIFY_ACCESS_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}